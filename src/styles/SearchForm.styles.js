import styled from "styled-components";

const SearchContainer = styled.div`
  max-width: 552px;
  margin: 0 auto;
`;

const SearchTitle = styled.div`
  text-align: center;
  font-size: 30px;
  font-weight: bold;
  margin-bottom: 40px;
`;

export const style = {
  SearchContainer,
  SearchTitle,
};
