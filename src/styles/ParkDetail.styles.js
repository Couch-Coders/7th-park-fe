import styled from "styled-components";

const ParkTitle = styled.div`
  color: #000;
  font-size: 25px;
  font-weight: bold;
  margin-top: 70px;
  margin-bottom: 25px;
`;
const ParkImage = styled.div``;
const ParkMap = styled.div`
  background-color: #eee;
  width: 100%;
  margin-left: 27px;
`;
const ParkLink = styled.div`
  font-size: 20px;
`;
const ParkAddr = styled.div`
  font-size: 20px;
`;
const ParkDesc = styled.div`
  font-size: 20px;
  margin-bottom: 70px;
`;

export const style = {
  ParkTitle,
  ParkImage,
  ParkMap,
  ParkLink,
  ParkAddr,
  ParkDesc,
};
