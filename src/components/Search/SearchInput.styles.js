import styled from "styled-components";

const SearchInputContainer = styled.div`
  width: 500px;
  margin: 40px auto;
`;

const SearchInputTitle = styled.div`
  font-size: 28px;
  font-weight: bold;
  text-align: center;
  margin: 20px 0;
  color: #495056;
`;

export const style = { SearchInputContainer, SearchInputTitle };
